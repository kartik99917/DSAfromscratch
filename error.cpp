#include <iostream> //I/P and O/P operations
#include <vector> //dynamic array vector
#include <string> // string class
#include <queue> // queue, priority_queue
#include <stack> // stack (LIFO)
#include <set> //ordered sets that store unique elements
#include <map> // ordered map that store key-value pairs
#include <unordered_map> //unordered map that store key-value pairs without any order
#include <unordered_set> //unordered sets that store unique elements without any order
#include <algorithm> // for algorithms like sort, reverse, etc.
#include <cmath> // for mathematical functions like sqrt, pow, etc.
#include <cstring> // for C-style string functions like strlen, strcpy, etc.
#include <iomanip> // for input/output manipulators like setprecision, setw, etc.

using namespace std;

int main () {
    int arr[2][3];
    cout << "Value at Index 1,2 is:" << arr[1][2] << endl;

    return 0;
}